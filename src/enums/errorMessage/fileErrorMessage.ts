export enum ERROR_FILE_REASON {
  CSV = "Only csv files are allowed",
  EXCEL = "Only excel files are allowed",
  NOT_ACCEPTABLE = "Input Data format not acceptable",
  EMPTY_FILE = "File not uploaded",
}
